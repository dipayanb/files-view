import { stringCapitalize } from '../../../helpers/string-capitalize';
import { module, test } from 'qunit';

module('Unit | Helper | string capitalize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringCapitalize([42]);
  assert.ok(result);
});
