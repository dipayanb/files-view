import { getSortingIcon } from '../../../helpers/get-sorting-icon';
import { module, test } from 'qunit';

module('Unit | Helper | get sorting icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getSortingIcon([42]);
  assert.ok(result);
});
