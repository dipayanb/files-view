import { getValueFromColumns } from '../../../helpers/get-value-from-columns';
import { module, test } from 'qunit';

module('Unit | Helper | get value from columns');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getValueFromColumns([42]);
  assert.ok(result);
});
