import { showDate } from '../../../helpers/show-date';
import { module, test } from 'qunit';

module('Unit | Helper | show date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showDate([42]);
  assert.ok(result);
});
