import { sizeHumanize } from '../../../helpers/size-humanize';
import { module, test } from 'qunit';

module('Unit | Helper | size humanize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sizeHumanize([42]);
  assert.ok(result);
});
