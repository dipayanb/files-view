import fileUploader from '../../../utils/file-uploader';
import { module, test } from 'qunit';

module('Unit | Utility | file uploader');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fileUploader();
  assert.ok(result);
});
